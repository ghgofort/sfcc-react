/**
 * @file ProductRef.js
 * @fileoverview - Contains a class representation of the OCAPI Shop API
 *    ProductRef (product reference) document type.
 */

/**
 * @class ProductRef
 * @classdesc A class representation of the OCAPI ProductRef document type that
 *    can be instantiated by passing an OCAPI ProductRef document as the only
 *    argument to the constructor.
 */
export default class ProductRef {
  /**
   * @constructor
   * @param {Object} args
   */
  constructor() {
    /** @todo : Implement ProductRef OCAPI document class. */
  }
}


